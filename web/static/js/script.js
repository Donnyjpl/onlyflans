$(document).ready(function() {
  // Inicialización del carousel
  $('#carouselflansindex').carousel();
});

 /*  document.addEventListener('DOMContentLoaded', function() {
    // Obtenemos el elemento del carousel
    const myCarouselElement = document.querySelector('carouselExampleControls');

    // Inicializamos el carousel utilizando el constructor Carousel de Bootstrap 5
    const carousel = new bootstrap.Carousel(myCarouselElement, {
        interval: 3000,  // Intervalo de cambio de slide en milisegundos
        pause: 'hover',  // Pausar el carousel al pasar el ratón por encima
        touch: false      // Deshabilitar la interacción táctil en dispositivos móviles
    });
}); */